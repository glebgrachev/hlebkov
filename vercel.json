{
  "buildCommand": "npm ci --include=dev && npm run build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/create-payment"
    }
  ]
}